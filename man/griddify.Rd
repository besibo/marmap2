% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/griddify.R
\name{griddify}
\alias{griddify}
\title{Grid irregularly spaced bathymetric data}
\usage{
griddify(
  x,
  nlon,
  nlat,
  lon = "lon",
  lat = "lat",
  depth = "depth",
  crs = 4326,
  interpolate = TRUE,
  radius = NULL,
  power = 2,
  class = c("tbl", "bathy", "spatraster")
)
}
\arguments{
\item{x}{A data frame/tibble with longitude, latitude, and depth columns, a
three-column matrix, a point \code{sf} object, a \code{terra::SpatRaster}, or a
historical \code{bathy} object.}

\item{nlon, nlat}{Integer. Number of longitude and latitude cells in the
target grid.}

\item{lon, lat, depth}{Character. Names of the longitude, latitude, and depth
columns when \code{x} is tabular.}

\item{crs}{Coordinate reference system assigned to the target grid. Can be a
CRS string such as \code{"EPSG:4326"} or a numeric EPSG code such as \code{4326}.}

\item{interpolate}{Logical. If \code{TRUE}, empty target cells are filled where
possible by inverse distance weighting from the input points.}

\item{radius}{Numeric. Search radius used for inverse distance weighting when
\code{interpolate = TRUE}. If \code{NULL}, a radius based on the target cell size is
used.}

\item{power}{Numeric. Distance weighting power passed to
\code{\link[terra:interpIDW]{terra::interpIDW}}.}

\item{class}{Character. Output class. \code{"tbl"} returns a tibble with columns
\code{lon}, \code{lat}, and \code{depth}; \code{"bathy"} returns a historical \code{bathy} matrix;
\code{"spatraster"} returns a \code{terra::SpatRaster}.}
}
\value{
A tibble by default, or an object of class \code{bathy} or
\code{terra::SpatRaster}.
}
\description{
\code{griddify()} converts irregularly spaced longitude/latitude/depth data to a
regular bathymetric grid. It is useful when external data are available as
scattered xyz points rather than as a regular raster-like grid.
}
\details{
Points falling in the same target cell are averaged. When
\code{interpolate = TRUE}, cells without observations are filled with inverse
distance weighting. This interpolation is intended as a pragmatic gridding
step for visualisation and exploratory mapping; it should not be interpreted
as a substitute for a domain-specific spatial interpolation model.
}
\examples{
set.seed(1)
xyz <- data.frame(
  lon = runif(100, -5, -3),
  lat = runif(100, 47, 49),
  depth = rnorm(100, -100, 30)
)

grid <- griddify(xyz, nlon = 30, nlat = 30)
summarise_bathy(grid)
}
\seealso{
\code{\link{as_bathy}}, \code{\link{as_spatraster}},
\code{\link{geom_bathy}}, \code{\link{quickplot_bathy}}
}
