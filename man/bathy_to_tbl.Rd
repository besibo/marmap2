% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bathy_tbl.R
\name{bathy_to_tbl}
\alias{bathy_to_tbl}
\alias{tbl_to_bathy}
\title{Convert between bathy objects and tibbles}
\usage{
bathy_to_tbl(x, names = c("lon", "lat", "depth"))

tbl_to_bathy(x, lon = "lon", lat = "lat", depth = "depth")
}
\arguments{
\item{x}{An object of class \code{bathy} for \code{bathy_to_tbl()}, or a table for
\code{tbl_to_bathy()}.}

\item{names}{Character vector of length 3 giving the output column names for
longitude, latitude, and depth/elevation.}

\item{lon, lat, depth}{Column names used by \code{tbl_to_bathy()}.}
}
\value{
\code{bathy_to_tbl()} returns a tibble. \code{tbl_to_bathy()} returns an object of
class \code{bathy}.
}
\description{
\code{bathy_to_tbl()} converts the historical matrix-based \code{bathy} class to a
long tibble with longitude, latitude, and depth/elevation columns.
\code{tbl_to_bathy()} converts a long table back to a \code{bathy} object.
}
\examples{
data(celt)

celt_tbl <- bathy_to_tbl(celt)
celt_tbl

celt_bathy <- tbl_to_bathy(celt_tbl)
class(celt_bathy)
}
\seealso{
\code{\link{as_bathy}}, \code{\link{as_xyz}}, \code{\link{get_noaa}},
\code{\link{get_gebco}}
}
