% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geom_coastline.R
\name{geom_coastline}
\alias{geom_coastline}
\title{Draw the coastline from bathymetric data}
\usage{
geom_coastline(
  mapping = NULL,
  data = NULL,
  ...,
  lon = "lon",
  lat = "lat",
  depth = "depth",
  colour = "black",
  linewidth = 0.6,
  linetype = "solid",
  na.rm = FALSE,
  show.legend = NA,
  inherit.aes = TRUE
)
}
\arguments{
\item{mapping}{Set of aesthetic mappings created by
\code{\link[ggplot2:aes]{ggplot2::aes}}. If \code{x}, \code{y}, or \code{z} are missing,
they default to \code{lon}, \code{lat}, and \code{depth}.}

\item{data}{Data to display in this layer. Defaults to the data inherited
from \code{\link[ggplot2:ggplot]{ggplot2::ggplot}}.}

\item{...}{Additional arguments passed to
\code{\link[ggplot2:geom_contour]{ggplot2::geom_contour}}.}

\item{lon, lat, depth}{Character. Names of the longitude, latitude, and depth
columns used when the corresponding aesthetics are not supplied in
\code{mapping}.}

\item{colour, linewidth, linetype}{Default line style. These can be changed to
customize the coastline.}

\item{na.rm, show.legend, inherit.aes}{Arguments passed to
\code{\link[ggplot2:geom_contour]{ggplot2::geom_contour}}.}
}
\value{
A ggplot2 layer.
}
\description{
\code{geom_coastline()} draws the \verb{0 m} contour line from a regular bathymetric
grid. It is a small convenience wrapper around
\code{\link[ggplot2:geom_contour]{ggplot2::geom_contour}} with
\code{breaks = 0}.
}
\examples{
\dontrun{
dat |>
  ggplot2::ggplot() +
  geom_bathy(ggplot2::aes(lon, lat, fill = depth), expand = FALSE) +
  geom_coastline()

dat |>
  ggplot2::ggplot() +
  geom_bathy(ggplot2::aes(lon, lat, fill = depth), expand = FALSE) +
  geom_coastline(colour = "grey25", linewidth = 0.6, linetype = "dashed")
}
}
\seealso{
\code{\link{geom_bathy}},
\code{\link[ggplot2:geom_contour]{ggplot2::geom_contour}}
}
