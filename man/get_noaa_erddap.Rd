% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_noaa_erddap.R
\name{get_noaa_erddap}
\alias{get_noaa_erddap}
\title{Download bathymetry from NOAA ETOPO 2022 through ERDDAP}
\usage{
get_noaa_erddap(
  lon = NULL,
  lat = NULL,
  lon1 = NULL,
  lon2 = NULL,
  lat1 = NULL,
  lat2 = NULL,
  resolution = 4,
  antimeridian = FALSE,
  keep = FALSE,
  path = NULL,
  progress = TRUE,
  timeout = 300,
  connect_timeout = 60,
  base_url = noaa_erddap_default_base_urls(),
  class = c("tbl", "bathy")
)
}
\arguments{
\item{lon}{Numeric vector of length 2 giving the longitude bounds in decimal
degrees. This is the recommended syntax.}

\item{lat}{Numeric vector of length 2 giving the latitude bounds in decimal
degrees. This is the recommended syntax.}

\item{lon1, lon2, lat1, lat2}{Explicit coordinate bounds, kept for compatibility
with older calling styles.}

\item{resolution}{Requested grid resolution in arc-minutes. The value is
converted to the nearest ERDDAP stride from the native 0.25 arc-minute
grid.}

\item{antimeridian}{Logical. Whether the requested region crosses the
antimeridian.}

\item{keep}{Logical. Whether to write the downloaded xyz table to disk.}

\item{path}{Directory used for cached csv files when \code{keep = TRUE}, and
where cached files are searched before downloading.}

\item{progress}{Logical. If \code{TRUE}, show curl's download progress when
the server provides enough information. If a precise progress bar is not
possible, the function still reports the expected grid size.}

\item{timeout}{Timeout in seconds for the ERDDAP download request. Defaults
to \code{300}.}

\item{connect_timeout}{Timeout in seconds for the initial connection to each
ERDDAP server. Defaults to \code{60}.}

\item{base_url}{ERDDAP griddap endpoint(s). Defaults to NOAA OceanWatch and
NOAA CoastWatch endpoints. If several endpoints are provided, they are
tried in order until one succeeds.}

\item{class}{Character. Class of the returned object. Use \code{"tbl"}
(default) to return a tibble with columns \code{lon}, \code{lat}, and
\code{depth}; use \code{"bathy"} to return a historical matrix of class
\code{bathy}.}
}
\value{
A tibble by default, or an object of class \code{bathy} when
\code{class = "bathy"}.
}
\description{
Experimental ERDDAP-based downloader for NOAA ETOPO 2022 bathymetry. It uses
the ERDDAP \code{griddap} endpoint, which can return NetCDF subsets directly and
can reduce download size by requesting one native grid point every \code{stride}
cells.
}
\details{
ETOPO 2022 is exposed by NOAA ERDDAP at a native resolution of 15 arc-seconds,
i.e. 0.25 arc-minutes. The \code{resolution} argument is converted to an ERDDAP
stride using:

\preformatted{
stride = round(resolution / 0.25)
}

The effective downloaded resolution is therefore \code{stride * 0.25}
arc-minutes. This is server-side sub-sampling, not local aggregation: ERDDAP
returns one native cell every \code{stride} cells. For local aggregation by mean or
median, download at the native resolution and use
\code{\link{reduce_bathy_resolution}} afterwards.

This function is intentionally separate from \code{\link{get_noaa}} while the
ERDDAP workflow is being evaluated.
}
\examples{
\dontrun{
dat <- get_noaa_erddap(
  lon = c(-5, 5),
  lat = c(40, 45),
  resolution = 1
)
}
}
\seealso{
\code{\link{get_noaa}}, \code{\link{reduce_bathy_resolution}},
\code{\link{read_bathy}}, \code{\link{geom_bathy}}
}
