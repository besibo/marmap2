% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/as_sf.R
\name{as_sf}
\alias{as_sf}
\title{Convert bathymetric data to sf}
\usage{
as_sf(x, lon = "lon", lat = "lat", crs = 4326, remove = FALSE, ...)
}
\arguments{
\item{x}{An object of class \code{bathy}, or a data frame/tibble containing
longitude and latitude columns.}

\item{lon, lat}{Character. Names of the longitude and latitude columns used
when \code{x} is a data frame or tibble.}

\item{crs}{Coordinate reference system assigned to the returned
\code{sf} object. Defaults to \code{4326}, i.e. WGS84 longitude/latitude.}

\item{remove}{Logical. If \code{TRUE}, the coordinate columns are removed
from the returned attribute table. Defaults to \code{FALSE}.}

\item{...}{Additional arguments passed to \code{\link[sf:st_as_sf]{sf::st_as_sf}}.}
}
\value{
An object of class \code{sf} with point geometries.
}
\description{
Converts an object of class \code{bathy}, or a long table containing
longitude and latitude columns, to an \code{sf} object with point geometries.
}
\examples{
xyz <- data.frame(
  lon = rep(c(-5, -4, -3), each = 3),
  lat = rep(c(48, 49, 50), times = 3),
  depth = c(-80, -70, -60, -120, -110, -100, -160, -150, -140)
)

xyz_sf <- as_sf(xyz)
class(xyz_sf)
}
\seealso{
\code{\link{bathy_to_tbl}}, \code{\link{tbl_to_bathy}},
\code{\link{as_spatraster}}, \code{\link{project_bathy}}
}
